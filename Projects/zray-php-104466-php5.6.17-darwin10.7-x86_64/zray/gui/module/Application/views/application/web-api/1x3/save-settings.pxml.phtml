	<responseData>
		<settings>
			<?php foreach ($settings as $key => $value) {
			echo "<setting>			 
			<name>{$key}</name>
			<value>{$value}</value>
			</setting>
			";
			}
			?>
		</settings>
	</responseData>